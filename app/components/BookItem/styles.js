import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  bookContainer: {
    padding: 16,
    borderWidth: 1,
    borderBottomWidth: 1,
    marginBottom: -1,
    borderColor: '#ccc',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  bookName: {
  },
  bookAuthor: {
  },
  bookGenre: {
  },
})
